<%= form_with(model: all, local: true) do |form| %>
  <% if all.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(all.errors.count, "error") %> prohibited this all from being saved:</h2>

      <ul>
      <% all.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :top %>
    <%= form.text_field :top, id: :all_top %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :all_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
